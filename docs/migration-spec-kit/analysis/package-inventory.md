# SSIS Package Inventory

> Auto-generated by Claude skill analysis. Update as new packages are added.

| # | Package Name | Description | Load Type | Source | Target | Complexity | Status |
|---|---|---|---|---|---|---|---|
| 1 | ETL_Load_Customers.dtsx | Incremental load of customer dimension from AdventureWorks to DW_Staging, followed by SCD Type 2 merge | Incremental | AdventureWorks.dbo.Customer | dim.Customer | Medium | Pending Migration |
| 2 | ETL_Load_Orders.dtsx | Incremental load of orders fact with region summary aggregation, error handling to flat file, and failure notification | Incremental | AdventureWorks.dbo.Orders | fact.Orders + fact.DailyRegionSummary | High | Pending Migration |

## Package Details

### ETL_Load_Customers.dtsx

**Control Flow:**
1. Execute SQL: Truncate staging.Customer
2. Execute SQL: Get last modified date from staging
3. Data Flow: Load customers (incremental, parameterized)
4. Execute SQL: Call sp_MergeCustomerDim (SCD Type 2)
5. Execute SQL: Audit row counts

**Data Flow Components:**
- OLE DB Source → Derived Column (audit fields, name cleanup) → Conditional Split (active/inactive) → Lookup (existing customer key) → OLE DB Destination

**Dependencies:**
- sp_MergeCustomerDim stored procedure
- staging.Customer table
- dim.Customer table
- audit.PackageExecution table

**Connection Managers:**
- OLEDB_Source_AdventureWorks (SQL Server, AdventureWorks)
- OLEDB_Dest_DWStaging (SQL Server, DW_Staging)

---

### ETL_Load_Orders.dtsx

**Control Flow:**
1. Sequence Container: Pre-Processing
   - Execute SQL: Get Max OrderID from staging
   - File System Task: Archive previous error log
2. Data Flow: Load orders (incremental, multi-output)
3. Execute SQL: Call sp_MergeOrderFact
4. Send Mail Task: Failure notification (on failure only)

**Data Flow Components:**
- OLE DB Source → Data Conversion → Derived Column (year, month, high-value flag, audit) → OLE DB Destination (staging.Orders)
- Branch: Aggregate (region summary) → OLE DB Destination (staging.OrderRegionSummary)
- Error Output: → Flat File Destination (error log CSV)

**Dependencies:**
- sp_MergeOrderFact stored procedure
- staging.Orders, staging.OrderRegionSummary tables
- fact.Orders, fact.DailyRegionSummary tables
- dim.Customer, dim.Date, dim.Region lookup tables
- ref.CurrencyRate reference table
- File share: \\FileServer\ETL\Logs\

**Connection Managers:**
- OLEDB_Source (SQL Server, AdventureWorks)
- OLEDB_Dest (SQL Server, DW_Staging)
- FlatFile_ErrorLog (CSV, network share)
